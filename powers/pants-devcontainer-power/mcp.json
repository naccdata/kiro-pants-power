{
  "mcpServers": {
    "pants-devcontainer": {
      "command": "python",
      "args": ["-m", "src.server"],
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  }
}
